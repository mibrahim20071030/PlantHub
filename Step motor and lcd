#include <LiquidCrystal.h>
#include <Stepper.h>

// LCD setup: RS, E, D4, D5, D6, D7
LiquidCrystal lcd(7, 6, 5, 4, 3, 2);

// Stepper setup: steps per revolution, and IN1–IN4 pins
const int stepsPerRevolution = 2048;
Stepper myStepper(stepsPerRevolution, 8, 10, 9, 11); // IN1, IN3, IN2, IN4

void setup() {
  // Initialize LCD
  lcd.begin(16, 2);        // 16 columns and 2 rows
  lcd.print("Stepper Start");

  // Initialize Stepper Motor
  myStepper.setSpeed(10);  // 10 RPM

  delay(2000);             // Wait 2 seconds before starting
}

void loop() {
   // Rotate 45 degrees forward
  lcd.clear();
  lcd.setCursor(0, 0);
  lcd.print("Watering Plant");
  myStepper.step(256);     // 45 degrees forward
  delay(5000);             // Hold for 5 seconds

  // Rotate 45 degrees backward (return to start)
  lcd.clear();
  lcd.setCursor(0, 0);
  lcd.print("Returning...");
  myStepper.step(-256);    // 45 degrees backward
  delay(2000);             // Wait before repeating
}
}
